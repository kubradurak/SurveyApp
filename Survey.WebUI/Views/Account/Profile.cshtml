@model Survey.Entities.User;

@{
    ViewData["Title"] = "Profile";
}
<h3>Profiliniz</h3>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FullName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Password)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>

        <tr>
            <td>
                @Html.DisplayFor(modelItem => Model.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => Model.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => Model.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => Model.Password)
            </td>

        </tr>

    </tbody>
</table>

<a asp-action="EditProfile" asp-controller="Account" asp-route-id="@Model.Id" class="btn btn-secondary">Profili güncelle</a>

<h2>Oyladığınız Anketler</h2>
